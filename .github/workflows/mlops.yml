name: IPL MLOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-train:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Task-5-End-to-End-MLOps-Pipeline

    env:
      MLFLOW_ALLOW_FILE_STORE: "true"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Train Model
        run: python train.py

      - name: Build Docker Image
        run: docker build -t ipl-mlops .
